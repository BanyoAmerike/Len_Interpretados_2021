
let input_value = document.getElementById("numero").value
let resultado = input_value * (input_value * input_value);

function cubo () {
    resultado.innerText = ver;
    document.body.append(resultado);
    alert(ver);
    
}

